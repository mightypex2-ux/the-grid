[package]
name = "grid-programs-zid"
version = "0.2.0"
edition = "2021"
description = "ZID (Zero Identity) program descriptor and messages"
license = "MIT OR Apache-2.0"

[dependencies]
grid-core = { path = "../../grid-core" }
serde = { version = "1", features = ["derive"] }
serde_bytes = "0.11"
