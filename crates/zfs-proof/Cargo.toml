[package]
name = "zfs-proof"
version = "0.1.0"
edition = "2021"
description = "ZFS pluggable Valid-Sector proof verification"
license = "MIT OR Apache-2.0"

[dependencies]
zfs-core = { path = "../zfs-core" }
zfs-crypto = { path = "../zfs-crypto" }
thiserror = "2"
