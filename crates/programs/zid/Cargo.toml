[package]
name = "programs-zid"
version = "0.1.0"
edition = "2021"
description = "ZID (Zero Identity) program descriptor and messages"
license = "MIT OR Apache-2.0"

[dependencies]
zfs-core = { path = "../../zfs-core" }
serde = { version = "1", features = ["derive"] }
serde_bytes = "0.11"
